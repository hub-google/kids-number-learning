# 兒童數字手寫與數數學習網頁 - 功能盤點與缺漏項目

經過詳細比對《系統需求書.md》與目前的程式碼實作狀態，大部分的核心功能（橫向版面、自訂範圍、每 3 行的英雄動畫、手寫變色回饋等）都已經順利完成。

但有以下 **3 個項目** 目前尚未完全符合需求書的規範，屬於還沒開發完全的功能：

### 1. 缺少「局部橡皮擦」功能
* **需求書對應 (3.1 & 3.4)**：「擦掉 (Erase)：提供單獨橡皮擦功能，可擦除局部或清除當前畫布內容。」
* **目前實作**：目前的 `TracingCanvas.jsx` 中，只有提供「🧹 擦掉重寫」按鈕（一鍵清除整個畫布並重置進度），還沒有實作讓小孩可以滑動手指「局部擦拭」錯誤筆跡的獨立橡皮擦模式。

### 2. 實體音效與語音播報沒有「同時」觸發
* **需求書對應 (3.1 & 3.3)**：完成手寫時需「播放拍拍手鼓掌聲」**且** 語音播報「你好厲害！」；英雄出現時需播放歡呼聲 **且** 語音播報英雄台詞。
* **目前實作**：在 `App.jsx` 中，語音播報（`speak()`）被寫在音效播放的 `.catch()` 區塊內當作備案。這導致如果 `clap.mp3` 或 `cheer.mp3` 成功播放，就不會發出「你好厲害！」等鼓勵語音。兩者應該要並行觸發。

### 3. 英雄語音播報的台詞缺少「英雄名稱」
* **需求書對應 (3.3)**：專屬英雄語音播報：「太棒了！變形金剛（/奧特曼）為你拍拍手！繼續加油！」。
* **目前實作**：`App.jsx` 中的 `handleTriggerHero` 事件裡，目前的語音寫死為 `speak("太棒了！為你拍拍手！繼續加油！")`，少帶入了當下隨機出現的英雄名稱（變形金剛或奧特曼）。

---

除此之外，像是 GitHub Pages 部署以及 `index.html` 的不快取 (Cache-Control) 機制等非功能需求都已經實作完畢。
